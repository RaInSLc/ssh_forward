# 通用 AI 工作流与归档规则

本项目中执行任务的所有 AI Agent 必须遵守本规则。规则适用于代码、文档、配置、分析、排障、评审与调研任务。

## 归档目标

1. 每个任务的全部过程必须在 `报告/ai_docs/` 中留下真实、可追溯的 Markdown 文档。
2. 文档按流程类别和任务开始日期分类，路径格式为：`报告/ai_docs/<流程类别>/YYYY-MM-DD/YYYY-MM-DD-任务主题.md`。
3. 同日同类别发生同名任务时，文件名追加两位序号，例如：`2026-08-13-02-任务主题.md`。
4. 任务主题使用简短的中文或 ASCII 连字符命名；不得在文件名中包含敏感信息。
5. 文档使用 UTF-8（无 BOM）和 LF 换行。

## 必需流程文档

每个任务均须创建以下文档；阶段不适用或未执行时，仍须创建该文档并写明原因、影响和后续处理。

| 流程类别 | 目录 | 记录内容 |
| --- | --- | --- |
| 计划 | `计划` | 目标、范围、前提、风险、实施和验证计划。 |
| 操作 | `操作` | 实际关键操作、修改文件、命令摘要、决定、失败与重试。 |
| 审计 | `审计` | 变更范围审查、风险、遗留问题、规则与安全合规检查。 |
| 测试 | `测试` | 执行的验证、命令、结果、未执行项及原因。 |
| 报告 | `报告` | 最终结果、交付物、限制、后续建议。 |

按任务需要创建下列扩展文档，且不得以扩展文档代替五项必需文档：

| 流程类别 | 目录 | 适用情形 |
| --- | --- | --- |
| 需求 | `需求` | 需求澄清、验收标准或范围变更。 |
| 调研 | `调研` | 代码库、依赖、方案或外部资料调查。 |
| 决策 | `决策` | 存在可选方案、取舍或用户确认。 |
| 问题记录 | `问题记录` | 阻塞、异常、失败、重试或待用户授权事项。 |
| 复盘 | `复盘` | 长任务、阶段性交付或需要总结改进时。 |

## 文档内容要求

1. 每份文档开头必须包含：任务名称、日期、状态、负责人（AI 或用户）、关联文件。
2. 内容只记录已知事实。不得伪造操作、测试、审计、用户确认或外部系统状态。
3. 禁止记录密钥、令牌、密码、私有连接串、个人数据及其他敏感信息；命令和输出只保留脱敏摘要。
4. 计划在开始实际修改前创建；操作文档在执行中持续更新；审计、测试和最终报告在交付前完成。
5. 先检查工作区上下文与现有未关联改动，再开始写入计划文档。不得覆盖、还原或移动用户的未关联改动。

## AI 辅助代码

1. AI 编写但不属于项目产品、测试、构建或部署必需内容的代码，必须保存到 `报告/ai_codes/YYYY-MM-DD/<两位序号-任务主题>/`。
2. 每个辅助代码目录必须包含 `README.md`，说明用途、运行方式、输入、输出、依赖和是否可安全删除。
3. 正式源码、测试、配置和正式文档保留在项目既有目录，不得为了归档而移动。
4. 除非用户明确要求，辅助代码不得被项目构建、发布或运行流程自动加载。

## 执行边界

1. 仅修改完成用户目标所必需的文件。
2. 遇到外部操作、不可逆操作、敏感信息、权限不足或范围不明时，先在 `问题记录` 中归档事实，再请求用户确认。
3. 交付前确认五项必需文档与实际变更和验证结果一致。
