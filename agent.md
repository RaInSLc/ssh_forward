# 通用 AI Agent 规则入口

所有 AI Agent 在开始任务前必须读取并完整遵循项目根目录的 `AGENTS.md`。

执行要求：

1. 先检查工作区状态和已有上下文，再创建 `报告/ai_docs/计划/YYYY-MM-DD/YYYY-MM-DD-任务主题.md`。
2. 在任务过程中同步维护 `操作` 文档；按需创建 `需求`、`调研`、`决策`、`问题记录` 与 `复盘` 文档。
3. 交付前必须创建 `审计`、`测试` 和 `报告` 文档，并确认五项必需文档均位于 `报告/ai_docs/<流程类别>/YYYY-MM-DD/`。
4. AI 生成的非项目代码必须归档到 `报告/ai_codes/YYYY-MM-DD/<两位序号-任务主题>/`，并附带 `README.md`。
5. 文档必须真实、脱敏，使用 UTF-8（无 BOM）与 LF 换行；不得覆盖用户未关联的改动。

本文件仅作便捷入口；`AGENTS.md` 是完整且优先的规则来源。
