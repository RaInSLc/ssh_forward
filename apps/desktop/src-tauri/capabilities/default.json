{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the SSH Forward window.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
