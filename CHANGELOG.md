# Changelog

All notable changes to this project will be documented in this file.

## [0.1.21] - 2026-09-19

### Fixed
- **修复窗口不可见时仍在空转的 1.5 秒轮询**：主界面的快照轮询此前无条件执行，窗口最小化或切到其它应用后照旧运行。每次轮询都会让后端读一次配置文件、反序列化、逐条探测隧道进程，再把完整配置经 IPC 送回并触发整棵组件树重渲染——即使用户只是把窗口开着不动，也是约 **2400 次/小时** 的磁盘读取。现在窗口不可见时跳过周期触发（不影响操作后主动发起的刷新），并在重新可见时立即补一次，无需等待下一个周期。
- **修复非 Windows 平台会生成一个 `ssh` 无法执行的 askpass 脚本**：密码认证的辅助脚本此前没有任何平台隔离，在 macOS/Linux 上会写出一个 `@echo off` + `powershell.exe` 的 `.cmd`，而那里的 `ssh` 需要可执行脚本加 shebang，结果是脚本被创建、`ssh` 拿不到密码、用户只看到一句含义不明的连接失败。该路径此前被上层（非 Windows 不提供密码选项）拦截而不可达，属于「依赖上层拦截来避免的下层缺陷」。现在脚本生成与调用点都按平台隔离，非 Windows 分支返回明确错误，不再留下跑不通的脚本。

### Internal
- **新增 `scripts/check-ipc-contract.mjs`（零依赖）**：校验前端与 Rust 之间的 IPC 契约。前端以字符串命令名调用 Tauri，与 `generate_handler!` 之间没有任何编译期约束——重命名一个命令，Rust 编译通过、`tsc --noEmit` 也通过，只在运行时以「命令不存在」暴露。脚本做双向差集检查（已注册但前端零引用 = 死命令；前端引用但未注册 = 拼写错误或已删除），并检查豁免表是否过期。已接入 CI 的静态检查作业（原「版本号一致性」作业更名为「静态一致性检查」）。
- 后端测试 66 → 67 项（新增 askpass 平台分叉用例），前端测试 26 → 28 项（新增 2 项窗口可见性轮询用例）。
- 手写 TS 契约补齐缺失字段：`Tunnel` 增加 `auto_start` / `auto_reconnect`，`Host.auth` 增加 `credential_id` / `encrypted_password`——此前类型描述与线格式不符，而 `tsc` 不会发现。
- `set_config_path` 与 `validate_config` 仍然注册但前端零引用，现已在契约脚本的豁免表中逐条登记原因（前者缺「选择配置文件」入口，后者缺「校验配置」入口）。**接线还是删除属产品决策**，豁免表的作用是让每一条不可达命令都是已知且有意为之，而不是悄悄累积。

## [0.1.20] - 2026-09-19

### Fixed
- **修复隧道诊断日志无上限增长**：日志写入此前没有任何容量控制，长时间运行的隧道（反复重连、keepalive 超时刷屏）会让文件持续膨胀，且删除隧道后文件永久残留，查看诊断时还会把整个文件读进内存。现在写入端在超过 2 MiB 时轮转一次（当前文件改名为 `.log.1`，只保留一代），删除隧道时一并清理当前与轮转后的日志，读取端改为从文件尾部按窗口读取、不足 200 行时才扩张窗口，不再整文件载入。
- **修复界面可能被过期的轮询响应回退**：快照轮询既被定时器触发也被每次操作等待，两次调用可以重叠，而响应没有序号校验。先发出的慢响应后到达时，会用较旧的数据覆盖较新的数据（例如刚创建完主机，列表却短暂回退到创建前的状态）。现在每次请求带单调递增序号，落地前比较并丢弃过期响应；过期请求的失败也不再弹出错误提示。
- **修复命令行启动的隧道污染用户 `~/.ssh/known_hosts`**：桌面端在 0.1.17 已改用应用私有 `known_hosts`，但命令行端（`ssh-forward start`）一直传空的启动选项，于是 OpenSSH 退回默认行为，把应用自己的信任记录写进**用户主目录的 `~/.ssh/known_hosts`**，同时完全不产生诊断日志。现在两端共用 core 的同一套路径推导（`RuntimePaths`），命令行启动的隧道改为写入与配置文件同级的 `known_hosts` 与 `logs/tunnel-<隧道 id>.log`，并在启动后打印日志路径。

### Changed
- **打开系统默认浏览器不再经过命令行解析**：此前用 `cmd /C start` 打开本地转发地址，`cmd.exe` 有自己的解析规则，Rust 的标准参数引号机制对它不完全生效，安全性只能依赖调用点的绑定地址白名单。现在改用 `ShellExecuteW`，从根上消除了命令行解析层，并新增 URL scheme 白名单（仅 `http` / `https`）。
- 诊断日志新增轮转文件 `logs/tunnel-<隧道 id>.log.1`，单个隧道的日志磁盘占用上界为 4 MiB。

### Internal
- 后端测试 38 → 66 项（config 9 + core 15 + ssh 17 + desktop 23 + cli 2；新增日志轮转、尺寸上界、重启后计数、清理、尾部窗口读取、多字节字符边界、URL scheme 白名单、运行期路径推导与命令行启动选项等用例），前端测试 24 → 26 项（新增 2 项轮询乱序响应用例）。
- **运行期路径推导下沉到 `ssh-forward-core`**（新增 `runtime` 模块）：`known_hosts` 与隧道日志路径此前由每个壳层各自实现，命令行端因此漏传。现在统一由 `RuntimePaths::for_tunnel` 推导、`RuntimePaths::options` 组装启动选项，壳层结构上无法再遗漏。
- `windows-sys` 新增 `Win32_UI_Shell` 与 `Win32_UI_WindowsAndMessaging` feature。

## [0.1.19] - 2026-09-17

### Fixed
- **修复设置面板的未保存输入被轮询还原**：主界面每 1.5 秒拉取一次快照，此前会无条件用后端值覆盖设置表单，导致用户在「全局网络与保活设置」中的编辑在保存前被静默还原（例如正把心跳探针周期从 15 改成 99，一秒后自己变回 15）。现在表单引入编辑态标记：用户一旦输入，轮询便不再覆盖；保存成功、放弃关闭或重新打开面板时恢复跟随后端。
- **修复隧道状态可能永久停留在「已停止」**：快照命令在探测隧道进程后会把整张状态表**整体写回**，覆盖同一时刻 `start_tunnel` 写入的 `starting` / `running`；而运行中的隧道不会在后续轮询里被重新写回，于是界面回退为「已停止」且无法自愈。现在只写回本次探测真正产生变化的条目，其余条目一律不动。
- **修复隧道诊断日志可能互相覆盖**：日志文件名此前由隧道名归一化生成（非字母数字字符一律替换为 `_`），`web api`、`web:api` 与 `web_api` 会落到同一个 `tunnel-web_api.log`，不同隧道的日志互相覆盖。现在改用隧道 `id`（UUID）命名，天然唯一。

### Changed
- 诊断日志文件名由 `logs/tunnel-<隧道名>.log` 变更为 `logs/tunnel-<隧道 id>.log`。旧文件不会被自动删除，如需清理可手动删除 `logs/` 目录；日志内容首行仍记录隧道名，便于辨认。

### Internal
- 隧道进程探测逻辑抽出 `ForwardProbe` 接口，使状态回收逻辑（上述第二项修复）可以在**不启动真实 OpenSSH 进程**的前提下被单元测试覆盖——此前该逻辑直接依赖 `OpenSshForward`，而构造它必须真的拉起一个 `ssh` 子进程。
- 后端测试 29 → 38 项，前端测试 21 → 24 项（新增 3 项轮询时序用例，覆盖「编辑中不被还原」「未编辑仍跟随后端」「放弃编辑后回到后端值」）。

## [0.1.18] - 2026-09-17

### Added
- **转发列表按服务器分组**：转发控制台此前把多台服务器的转发平铺在一起，条目一多就难以辨认归属。现在按所属服务器归类，组头显示服务器名、`用户名@主机:端口`、转发条数与运行中条数，点击组头可折叠/展开（`aria-expanded` 同步）。头部新增「分组」开关，默认开启，状态持久化；关闭后退化为原有的平铺列表。
- **「创建副本」**：新增以现有转发为模板创建新条目的入口（卡片视图为「创建副本」，表格视图为「副本」）。副本会完整保留转发类型、本地/远端端点、`GatewayPorts`、自定义 `-o` 参数与「启动后自动打开浏览器」等全部字段，并以**新建**方式打开表单，不会覆盖原条目。副本名自动去重：`web-副本`、`web-副本2`、`web-副本3`……
  - 副本是**保真**的：转发列表并不按类型过滤，基础模式下同样会显示反向/动态隧道，而基础模式的保存路径会把转发类型回退为本地转发、并丢弃 `GatewayPorts` 与自定义 `-o`。因此当源条目用到其中任何一项时，副本会自动打开高级模式，避免副本被静默降级。

### Changed
- **端口冲突不再静默**：转发表单在本地端口与既有条目重复时给出显式警告（副本会原样复制端口，尤其容易撞上）。此前要等到点「启动」才会失败，且提示不指向真正原因。
- **表格视图列宽调整**：操作列 26% → 34%、转发链路 40% → 32%，以容纳新增的「副本」按钮（该按钮容器为 `nowrap`，不加宽会挤压）。

### Notes
- 分组状态存于浏览器本地存储键 `ssh-forward-group-by-host`：**默认开启**（键不存在即视为开启），仅在用户显式关闭时写入 `"false"`。折叠状态不持久化，避免下次启动时「内容不见了」的困惑。
- 副本的端口与原条目相同，因此**两者无法同时启动**——这一点由表单内的冲突警告显式告知，而非静默处理。

## [0.1.17] - 2026-09-17

### Fixed
- **修复基础模式保存会静默清空高级配置**：此前在基础模式下编辑服务器时，跳板机、`ProxyCommand`、证书、压缩与自定义 `-o` 参数会被一并写成 `null`，用户只是修改端口就会丢失这些设置。现在这些未在表单中渲染的字段会沿用原有值。
- **修复 Host Key 处理污染用户系统文件**：不再把 `ssh-keyscan` 结果写入用户主目录的 `~/.ssh/known_hosts`，改为写入应用私有文件（与 `config.json` 同目录），并在设置界面展示该路径。
- **修复 macOS 上"密码"认证被作为默认推荐**：密码认证依赖 Windows DPAPI，非 Windows 平台不再提供该选项，默认认证方式改为 SSH Agent。
- **修复关于弹窗版本号兜底值过期**（此前硬编码为 `0.1.15`）。
- **修复 `ssh-forward-ssh` 的 `windows-sys` feature 声明不足**：该 crate 用到的 `CreateJobObjectW` 实际由 `Win32_Security` 门控、`JOBOBJECT_EXTENDED_LIMIT_INFORMATION` 由 `Win32_System_Threading` 门控，此前仅靠工作区其它 crate 的 feature 合并才可见，单独 `cargo build -p ssh-forward-ssh` 会报 `E0432`。现已显式声明。
- **修复 `.gitignore` 误忽略 `examples/config.json`**：原模式 `config.json` 未锚定仓库根目录，会匹配任意层级的同名文件，导致本应入库的示例配置模板从未被跟踪。现改为 `/config.json`、`/config.json.lock`、`/known_hosts`。

### Added
- **真实错误诊断**：OpenSSH 的 stderr 不再被丢弃，改为实时捕获并按 Tunnel 落盘到 `logs/tunnel-<名称>.log`。Tunnel 异常退出时，错误卡片直接展示 OpenSSH 的原始输出尾部，并提供"查看诊断日志"弹窗与一键复制。
- **主机密钥校验策略三态化**：`settings.strict_host_key_checking`（布尔）升级为 `settings.host_key_policy`，可选 `accept_new`（默认，首次自动登记、变更则拒绝）、`strict`（未知主机直接拒绝）、`insecure`（完全不校验，需二次确认风险）。
- **跨进程配置文件锁**：`load → 修改 → save` 全程持有文件锁，避免两个应用实例或 GUI 与 CLI 并发写入时互相覆盖。
- **单实例运行**：重复启动会把已有窗口带到前台，不再产生第二个实例抢占同一端口。
- **配置文件版本迁移机制**：新增 `migrate()`，v1 配置在读取时自动升级到 v2，无需手工改动。
- **Unix 平台配置文件权限收紧**为 `0600`。

### Changed
- **配置写入路径统一**：桌面端不再内联实现服务器/Tunnel 的增改逻辑，改为复用 `ssh-forward-core` 的 `upsert_host` / `upsert_tunnel`，与 CLI 共享同一套规则；编辑服务器时留空密码表示沿用已保存的密文，不再强制重新输入。
- **错误类型细化**：新增 `Conflict` / `Disabled` / `CredentialMissing` / `Invalid` 变体，不再把"仍有关联 Tunnel""目标被禁用"一律归为 `NotFound` / `AlreadyExists`。
- **OpenSSH 启动参数改用 `ForwardSpec` 结构体传递**，消除 8 个位置参数带来的错位风险。
- 全局网络与保活设置入口改为常驻，基础模式下也可调整（主机密钥策略属于安全项，不应被高级模式隐藏）。
- CLI 的 `start` 等待改为 `thread::park()`，不再每秒空转轮询。

### Notes
- 配置文件版本号由 `1` 升为 `2`。旧版应用无法读取新版配置，新版应用可自动读取并升级旧版配置。
- 已有配置中 `strict_host_key_checking: false` 会迁移为 `host_key_policy: "insecure"`，以忠实保留原有行为，界面上会以风险提示形式标出。

## [0.1.16] - 2026-08-18

### Optimized
- **极窄窗口自适应与表格列重构**：
  - 将表格拆分的「本地端口」与「远端目标」合并为单列「转发链路 / 路由」，将表格精简为 4 列，彻底消除横向滚动条。
  - 侧边栏宽度与工作区内边距弹性紧凑化，在 800px 甚至更窄窗口下 100% 完整显示所有信息与操作按钮。
  - 窗口最小安全尺寸调整为 `800x560`，默认尺寸 `1060x720`。

## [0.1.15] - 2026-08-18

### Added
- **自适应响应式布局与表格视图**：针对窄屏或小窗口宽度自动优化降级为紧凑表格列表视图（Table View），彻底修复按钮文字换行折叠和排版拥挤问题，并支持卡片/表格视图手动切换。
- **基础模式 vs 高级设置模式分级**：
  - **基础模式 (默认)**：保持 `0.1.13` 的极简体验，界面无干扰、表单简洁专注。
  - **高级设置模式 (一键切换)**：开启后全面解锁 `0.1.14` 的 SOCKS5 动态代理、远程反向穿透、跳板机、证书、数据压缩与全局保活等专业网络设置。

## [0.1.14] - 2026-08-18

### Added
- **连接保活与高可用防假死**：引入 `ServerAliveInterval`（默认 15s）、`ServerAliveCountMax`（默认 3 次）与 `TCPKeepAlive`（默认开启），彻底解决 NAT/防火墙静默丢包导致的 SSH 假死。
- **动态端口转发 (SOCKS5 代理 `-D`)**：新增 `Dynamic` 隧道模式，仅需在本地指定端口即可建立全功能 SOCKS5 代理网关，直接访问远程全网服务。
- **远程反向端口转发 (`-R`)**：新增 `Remote` 隧道模式，支持内网穿透将本地服务端口暴露到公网服务器。
- **跳板机与代理穿透**：主机支持关联级联跳板机（`ProxyJump` / `-J`）与自定义前置代理命令（`ProxyCommand`）。
- **局域网共享支持**：支持 `GatewayPorts` / 绑定 `0.0.0.0`，允许局域网同伴设备直接访问转发端口。
- **认证隔离与安全进阶**：支持 `IdentitiesOnly=yes`（避免遍历 SSH-Agent 触发认证次数超限）及 CA 签发的用户证书认证（`CertificateFile`）。
- **传输性能优化**：支持数据流压缩（`Compression` / `-C`），提升高延迟/弱网传输速率。
- **自定义 OpenSSH 参数透传**：主机和隧道均支持配置任意自定义 `-o Option=Value` 参数列表。
- **界面与交互重塑**：新增模式分段切换、折叠式高级设置 Accordion、全局网络设置抽屉及彩色模式徽章。

## [0.1.13] - 2026-08-17

### Added
- 集成 Tauri v2 官方在线更新机制（`tauri-plugin-updater` & `@tauri-apps/plugin-updater`）。
- 桌面端关于弹窗支持检查 GitHub Releases 更新、展示版本更新说明、下载进度条与一键更新重启。
- GitHub Actions CI/CD 流水线支持私钥数字签名打包并自动生成跨平台 `latest.json` 更新清单资产。
- 转发配置本地地址支持下拉选择（`127.0.0.1`、`0.0.0.0`、`localhost`）。
- 新建 Tunnel 支持自动探测并分配空闲随机端口，表单新增“🎲 随机”端口分配按钮。
- 后端新增 `get_available_port` 系统空闲端口原子探测命令。
- 添加服务器默认认证方式调整为“密码”；增加 SSH Agent 前置使用条件说明与私钥/密码输入提示。
- Tunnel 启动失败时根据认证类型输出精准诊断指引，并在错误卡片提供一键跳转编辑服务器的快捷入口。
- `.gitignore` 增加 `.tauri/` 和私钥文件隔离规则。

### Fixed
- 修复 Windows 下软件意外关闭或直接关闭导致子进程 `ssh.exe` 变成孤儿进程持续占用端口的问题（引入 Windows 内核 Job Object 机制 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` + Tauri 窗口与退出生命周期双重自动清理）。
- 修复关于弹窗中版本号未动态获取的问题，由后端 `Snapshot` 动态返回 `env!("CARGO_PKG_VERSION")`。
- 优化检查更新异常捕获，屏蔽云端暂未上传本平台清单时的报错提示，提供友好的“已是最新版本”提示。

## [0.1.11] - 2026-08-17

### Fixed
- 修复 Windows 下启动 Tunnel 时弹出 `ssh-keyscan.exe` 黑色控制台窗口的问题（增加 `CREATE_NO_WINDOW` 标记）。
- 修复 `ssh-keyscan` 强制限定 `-t ed25519` 导致 RSA/ECDSA 密钥类型服务器无法获取 Host Key 并启动失败的问题。
- 将 OpenSSH 连接策略更新为 `StrictHostKeyChecking=accept-new`，实现首次连接自动安全登记 Host Key，避免无谓阻塞与扫描失败拦截。
- 完善 `start_tunnel` 启动前置阶段的错误捕获与状态同步机制。

## [0.1.10] - 2026-08-15

### Added
- 桌面端 UI 增加删除服务器 (Host) 和删除转发 (Tunnel) 的操作按钮与二次确认防呆逻辑。
- 服务器删除前增加关联 Tunnel 关联度的校验拦截提示。
- README.md 新增 CLI 命令行工具使用说明指南、Windows 安装版配置文件保存路径说明以及 DPAPI 密码跨设备解密限制提示。

### Changed
- 升级项目 workspace 及桌面端应用版本号至 0.1.10。

## [0.1.9] - 2026-08-13

### Added
- 初始版本发布，支持 SSH Local Forward 端口转发管理与桌面控制台。
