# 发布 v0.1.21 结果报告

- 任务名称：发布 v0.1.21 并监控打包
- 日期：2026-09-21
- 状态：部分完成
- 负责人：AI
- 关联文件：提交 `2177a76`、标签 `v0.1.21`、GitHub Release `v0.1.21`、GitHub Actions 运行 `35553661375`

## 交付结果

- 已提交并推送 `main`：`2177a76`（`feat(release): 发布 v0.1.21`）。
- 已创建并推送带注释标签 `v0.1.21`。
- GitHub Release 已发布：https://github.com/RaInSLc/ssh_forward/releases/tag/v0.1.21
- GitHub Actions 打包运行成功：https://github.com/RaInSLc/ssh_forward/actions/runs/35553661375
- Windows x64、macOS x64、macOS aarch64 的安装包和便携包均已上传。

## 限制

- 本次 Release 的在线更新清单为空：`latest.json.platforms` 为 `{}`。
- 根因是构建未产出 `.sig` 签名资产，无法生成 Tauri Updater 所需的平台签名记录。
- 因此手动下载安装可用，但客户端自动更新不可用。

## 后续建议

1. 在仓库 GitHub Actions Secrets 配置有效的 `TAURI_SIGNING_PRIVATE_KEY`，必要时配置 `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`。
2. 在 `release.yml` 中增加签名资产和 `latest.json.platforms` 非空的强制验证，避免空更新清单发布。
3. 完成机密配置后使用递增版本重新发布，不能复用 `v0.1.21`。
