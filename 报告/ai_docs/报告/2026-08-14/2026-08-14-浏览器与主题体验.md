# 浏览器与主题体验报告

- 任务名称：浏览器与主题体验
- 日期：2026-08-14
- 状态：完成
- 负责人：AI
- 关联文件：`apps/desktop/src/App.tsx`、`apps/desktop/src/styles.css`、`apps/desktop/src-tauri/src/main.rs`

## 交付结果

- 每条运行中的 Tunnel 新增“打开浏览器”按钮，使用系统默认浏览器打开本地转发 HTTP 地址。
- Tunnel 编辑窗口新增“启动成功后自动打开浏览器”选项，并持久化到配置。
- 主界面更新为信息更少的卡片式布局，默认日间模式，提供日间/夜间模式切换并保存用户选择。
- 新版 Release 位于 `target/release/SSH-Forward-GUI-v0.1.0-browser-theme.exe`，构建产物未提交到 Git。

## 限制

- 浏览器打开 URL 默认使用 HTTP，且只允许本地绑定地址。
