# 修复密码表单版本混淆

- 任务名称：修复密码表单版本混淆
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`target/release/SSH-Forward-GUI-v0.1.0-dpapi.exe`、`apps/desktop/src/App.tsx`

## 结果

截图中的“只有私钥路径”界面来自旧 Debug 程序，不是当前功能版本。已生成带可见版本标识的独立 Release 程序：

`target/release/SSH-Forward-GUI-v0.1.0-dpapi.exe`

启动该程序后，品牌下方会显示 `GUI v0.1.0-dpapi`。服务器表单包含认证方式下拉框，选择“密码（加密保存到配置）”后显示密码输入框。

## 使用要求

关闭所有旧 SSH Forward 窗口，不要打开 `target/debug/ssh-forward-desktop.exe`。仅使用本报告指定的版本化 Release 文件。
