# 修复旧密码配置阻塞

- 任务名称：修复旧密码配置阻塞
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`target/release/SSH-Forward-GUI-v0.1.0-dpapi-migration.exe`

## 结果

已修复旧密码配置导致整个 Host/Tunnel 列表为空、新服务器无法保存的问题。

请使用迁移兼容版本：

`target/release/SSH-Forward-GUI-v0.1.0-dpapi-migration.exe`

旧密码 Host 会重新显示。点击它进行编辑，选择密码认证并重新输入密码后保存，即可转换为 DPAPI 加密的 `encrypted_password` 配置。此后可以正常启动 Tunnel。

## 说明

旧 `credential_id` 不包含可恢复的密码；兼容版只能让配置加载并让用户重新输入密码完成迁移，不能自动找回旧密码。
