# 修复桌面启动资源

- 任务名称：修复桌面启动资源
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`target/release/ssh-forward-desktop.exe`、`README.md`

## 结果

已修复桌面端直接运行时访问未启动 `localhost:1420` 的问题。

请使用 Release Tauri Build 产物：`target/release/ssh-forward-desktop.exe`。该程序已验证可启动，且不会依赖 Vite 开发服务器。

## 使用说明

- 直接运行：`target/release/ssh-forward-desktop.exe`。
- 热更新开发：在 `apps/desktop` 执行 `node_modules/.bin/tauri.cmd dev`。
- 不要直接运行 `target/debug/ssh-forward-desktop.exe`，除非正在使用 Tauri 开发命令启动 Vite 服务。

## 限制

当前交付为独立 Release 可执行文件，尚未生成 MSI 或 NSIS 安装包。
