# 任务操作

- 任务名称：创建通用 AI Skill 与 Agent 归档规则
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`AGENTS.md`、`agent.md`、`.opencode/skills/ai-report-archive/SKILL.md`

## 已执行操作

1. 读取现有 `AGENTS.md` 与 `agent.md`，确认原规则采用按日期聚合的五文档结构。
2. 将根规则更新为按流程类别和日期分类的 `报告/ai_docs/<流程类别>/YYYY-MM-DD/` 结构。
3. 补充需求、调研、决策、问题记录、复盘五类按需流程文档规则。
4. 更新 `agent.md` 作为根规则的简明入口。
5. 创建 `.opencode/skills/ai-report-archive/SKILL.md`，将归档流程提供给 OpenCode 技能加载器。

## 关键决定

- 新规则统一使用用户指定的 `报告/ai_docs/` 路径；保留现有历史报告但不迁移，避免修改既有记录。
- 仅将非产品、非测试、非构建、非部署的 AI 辅助代码归档到 `报告/ai_codes/`。
