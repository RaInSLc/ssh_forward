# 修复桌面启动资源

- 任务名称：修复桌面启动资源
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`apps/desktop/src-tauri/tauri.conf.json`、`README.md`、`apps/desktop/dist/`

## 执行记录

1. 用户截图显示 SSH Forward 窗口加载 `localhost` 时被拒绝连接。
2. 已核对 Tauri 配置，确认 `devUrl` 指向 `http://localhost:1420`；普通 Cargo Debug 可执行文件会使用此开发 URL，但直接启动时没有 Vite 服务运行。
3. 将使用项目内 Tauri CLI 的构建模式创建加载 `frontendDist` 的独立桌面端产物，并更正 README 中对 Debug 可执行文件的描述。
4. 已执行 `tauri.cmd build --debug --no-bundle`，确认 Debug Tauri Build 仍使用开发配置；随后执行标准 `tauri.cmd build --no-bundle`，成功生成 Release 程序 `target/release/ssh-forward-desktop.exe`，该模式加载 `frontendDist` 而非 Vite `devUrl`。
5. 已更新 README：热更新开发使用 `tauri.cmd dev`；直接运行使用 Release Tauri Build 产物；普通 Cargo Debug 桌面端可执行文件不再被标为可独立运行。
