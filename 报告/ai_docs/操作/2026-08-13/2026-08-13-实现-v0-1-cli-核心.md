# 实现 V0.1 CLI 核心

- 任务名称：实现 V0.1 CLI 核心
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`报告/ai_docs/方案/初步设计.md`、`Cargo.toml`、`crates/`、`apps/cli/`、`schemas/config-v1.schema.json`、`README.md`

## 执行记录

1. 已读取项目归档规则、编码保护规则与初步设计文档。
2. 已确认当前目录不是 Git 仓库；未发现产品源码、构建清单或可识别的未提交改动。既有 `报告/` 和 `.opencode/` 文件保持不变。
3. 已确认本机可用 Rust 1.96.0、Cargo 1.96.0 和 Windows OpenSSH 9.5p1。
4. 已将首个交付边界设为 V0.1 的 CLI 核心基础：配置、校验、Host/Local Tunnel CRUD 与 OpenSSH 本地转发。GUI、凭据库及其他后续功能不在本次改动范围。
5. 已创建 Rust workspace：`ssh-forward-config` 负责版本化 JSON 模型、原子写入和语义校验；`ssh-forward-ssh` 负责 OpenSSH 参数构建与本地端口预检查；`ssh-forward-core` 提供 Host/Tunnel 操作；`apps/cli` 提供命令行入口。
6. 已新增 `schemas/config-v1.schema.json`、`examples/config.json` 和 `README.md`。示例使用不可路由的测试域名，未写入任何真实连接地址、凭据或私钥内容。
7. Cargo 在项目内 `.cargo/` 下载构建依赖，并生成 `Cargo.lock`、`target/` 构建产物；未向项目目录外写入依赖或构建缓存。
8. 首次 `cargo fmt --check` 报告格式差异，首次 CLI 冒烟检查发现一个未使用导入；已使用 `cargo fmt` 统一格式并删除该导入。首次 Clippy 发现两个无行为差异的表达式简化建议，已按建议修正。
9. 已完成格式、单元测试、Clippy、示例配置校验、列表命令以及临时配置的 Host/Tunnel 增删查验证。安全测试确认 `0.0.0.0` 本地绑定被拒绝；临时验证配置已删除。
