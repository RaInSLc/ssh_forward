# 补全 README 编译开发启动说明

- 任务名称：补全 README 编译开发启动说明
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`README.md`、`apps/desktop/package.json`、`apps/desktop/src-tauri/tauri.conf.json`

## 执行记录

1. 已读取 README、workspace、桌面端 npm scripts 与 Tauri 配置。
2. 已确认现有 README 只列出预构建前端资源后的 `cargo run` 方式，缺少 Vite 热更新开发、Release 编译、测试检查和打包说明。
3. 已确认 Tauri 配置的 `beforeDevCommand` 是 `npm run dev`，因此需要项目内 Tauri CLI 来正确编排前端开发服务器和桌面窗口。
4. 已在 `apps/desktop` 安装并锁定项目内 `@tauri-apps/cli` 2.11.4。尝试通过 npm script 调用 Tauri CLI 时，当前 Windows 的 npm PowerShell 包装发生命令解析冲突并显示 npm 帮助；已移除不可验证的脚本。直接调用项目内 `node_modules/.bin/tauri.cmd dev --help` 已通过，因此 README 使用该 Windows 入口进行开发和打包，不依赖全局 CLI 或 npm 参数转发。
5. 已补全 README 的环境要求、首次准备、CLI Debug/Release 编译与使用、GUI 热更新开发、GUI Debug/Release 编译、Windows 打包以及检查测试说明。
