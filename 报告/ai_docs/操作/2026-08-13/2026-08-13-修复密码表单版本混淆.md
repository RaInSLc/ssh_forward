# 修复密码表单版本混淆

- 任务名称：修复密码表单版本混淆
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`apps/desktop/src/App.tsx`、`target/release/`

## 执行记录

1. 用户截图显示旧 Host 表单，只有私钥路径输入，没有认证方式下拉框或密码输入。
2. 已确认当前源码包含认证方式与密码输入文本；旧 Debug 可执行文件早于当前 Release 程序，因此截图所示为旧 Debug 版本。
3. 将增加 GUI 版本标识并创建单独命名的 Release 交付文件，避免 Debug 与 Release 文件混淆。
4. 已在 GUI 品牌副标题加入 `GUI v0.1.0-dpapi` 可见版本标识；前端类型检查与生产构建通过，构建资源确认包含版本、认证方式和密码认证选项文本。
5. 初次 Release 构建命令因 PowerShell 内联字符串语法错误未启动；已使用简化命令重试并成功构建。已复制为 `target/release/SSH-Forward-GUI-v0.1.0-dpapi.exe`。
6. 新命名 Release 文件独立启动检查通过，测试窗口已停止。
