# 实现桌面 GUI

- 任务名称：实现桌面 GUI
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`Cargo.toml`、`apps/desktop/`、`crates/ssh-forward-core/`、`crates/ssh-forward-config/`、`README.md`

## 执行记录

1. 已读取项目归档与中文编码规则、初步设计中的主界面及表单设计，以及现有 CLI 和 Core 接口。
2. 已确认尚无 `apps/desktop` 工程；本机可用 Node 22.14、npm 10.9、Rust/Cargo 1.96，未安装全局 Tauri CLI。
3. 决定使用项目内 npm 和 Cargo 依赖创建 Tauri 2 + React + TypeScript 桌面端，避免使用或写入项目目录外的应用依赖、代码和配置。
4. 已创建 Tauri IPC 命令层和 React 管理界面。前端生产构建成功；首次 Tauri 后端检查因缺少 Windows 图标资源 `src-tauri/icons/icon.ico` 停止，尚未执行到 Rust 应用代码检查阶段。
5. 已新增项目内最小 Windows 图标资源并在 Tauri 配置中声明。随后 `cargo check -p ssh-forward-desktop` 和 `npm run check` 均已通过。
6. 已在 README 增加桌面端 GUI 的依赖安装、前端构建和启动说明，并说明当前的运行时功能边界。
7. 完整 workspace 的格式检查、单元测试和严格 Clippy，以及前端生产构建均已通过。审查时发现默认配置路径原先取决于启动工作目录，已改为由 `CARGO_MANIFEST_DIR` 定位项目根目录的 `config.json`，避免打包或不同启动目录导致行为不一致。
