# 发布 v0.1.21 操作记录

- 任务名称：发布 v0.1.21 并监控打包
- 日期：2026-09-21
- 状态：已完成（在线更新存在外部配置阻塞）
- 负责人：AI
- 关联文件：`Cargo.toml`、`apps/desktop/package.json`、`apps/desktop/src-tauri/tauri.conf.json`、`CHANGELOG.md`、`.github/workflows/ci.yml`

## 已确认上下文

- 分支为 `main`，开始时与 `origin/main` 同步。
- 工作区已有 18 个待发布的产品、测试、配置与脚本变更；未还原或覆盖任何既有改动。
- 当前版本三处均为 `0.1.21`，变更日志包含 `0.1.21`、`0.1.20` 和 `0.1.19` 的发布说明。
- 远程已有最新标签为 `v0.1.18`；尚不存在 `v0.1.21`。
- GitHub CLI 认证状态正常；`Release desktop app` 工作流处于启用状态。

## 操作日志

- 已在执行关键发布操作前创建计划和操作归档文档。
- 已完成发布前验证：版本号校验、IPC 契约校验、`cargo fmt --all -- --check`、`cargo clippy --workspace -- -D warnings`、`cargo test --workspace`、`npm --prefix apps\\desktop run check`、`npm --prefix apps\\desktop test` 以及 `npm --prefix apps\\desktop run build` 均成功。
- Rust 测试期间出现 Windows 增量编译缓存复制失败（`os error 5`）警告；所有测试完成且命令返回成功，未产生产品代码诊断。
- 前端测试结果为 5 个测试文件、28 项测试全部通过；生产构建成功。
- 已创建发布提交 `2177a76`（`feat(release): 发布 v0.1.21`）并成功推送至 `origin/main`。
- 推送前使用远程标签查询确认 `v0.1.21` 不存在；已创建带注释标签 `v0.1.21` 并成功推送至 `origin`。
- 标签推送触发的 `Release desktop app` 工作流 `35553661375` 已成功完成：Windows x64、macOS x64、macOS aarch64 构建作业及 Release 汇总作业均成功。
- 已确认 Release `v0.1.21` 发布且存在 7 个安装资产及 `latest.json`。
- 发布后下载并检查 `latest.json`，发现其 `platforms` 为 `{}`；Release 中不存在 `.sig` 签名资产。已创建问题记录说明在线更新不可用、影响和所需的外部机密配置。
