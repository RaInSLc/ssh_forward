# 浏览器与主题体验

- 任务名称：浏览器与主题体验
- 日期：2026-08-14
- 状态：进行中
- 负责人：AI
- 关联文件：`apps/desktop/src/App.tsx`、`apps/desktop/src/styles.css`、`apps/desktop/src-tauri/src/main.rs`、`crates/ssh-forward-config/src/model.rs`

## 执行记录

1. 已检查工作区，开始时没有未提交变更。
2. 已确认当前 Tunnel 模型没有浏览器自动打开字段，前端为压缩的单组件布局且默认深色视觉。
3. 计划将自动浏览器行为作为 Tunnel 的持久化布尔配置，并将主题选择保存到前端本地存储。
4. 已为 Tunnel 增加 `auto_open_browser` 兼容字段，创建和编辑流程均可保存该选项；启动成功后通过系统默认浏览器打开仅限 `127.0.0.1` 或 `localhost` 的 HTTP 地址。
5. 已重构桌面主视图为紧凑 Tunnel 卡片，增加打开浏览器按钮、自动打开复选框和日间/夜间主题切换；日间模式为默认主题，用户选择保存在本地存储。
6. 已完成 Rust 单元测试、严格 Clippy、TypeScript 检查和前端构建，均通过。
7. 已构建新版 Tauri Release 并通过启动存活检查；已确认敏感配置和 Release 构建产物均被 Git 忽略。
