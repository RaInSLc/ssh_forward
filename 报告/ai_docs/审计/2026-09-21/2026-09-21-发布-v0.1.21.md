# 发布 v0.1.21 审计

- 任务名称：发布 v0.1.21 并监控打包
- 日期：2026-09-21
- 状态：部分通过
- 负责人：AI
- 关联文件：提交 `2177a76`、标签 `v0.1.21`、`.github/workflows/release.yml`、GitHub Actions 运行 `35553661375`

## 变更范围审查

- 发布提交包含已有的功能、测试、版本、变更日志和 CI 静态契约检查变更，共 18 个产品相关文件。
- 产品版本在 `Cargo.toml`、`apps/desktop/package.json` 与 `apps/desktop/src-tauri/tauri.conf.json` 中均为 `0.1.21`；`CHANGELOG.md` 含唯一的对应条目。
- 提交已推送到 `origin/main`，本地 `HEAD` 与远程 `main` 均为 `2177a76`；标签 `v0.1.21` 指向该发布提交。

## 发布与安全审查

- 标签在创建前已确认远程不存在，符合不可重用旧标签和版本递增规则。
- 发布流程没有在归档中记录任何机密值；GitHub Actions 仅以仓库机密名注入签名参数。
- 本次 Release 的手动安装资产完整，Windows x64、macOS x64、macOS aarch64 三个平台构建均成功。
- 风险：未发现 `.sig` 签名资产，导致 `latest.json` 的 `platforms` 为空；在线自动更新不可用。该风险已记录在 `问题记录/2026-09-21/2026-09-21-v0.1.21-在线更新签名缺失.md`。

## 结论

手动安装包发布成功，在线更新发布不完整。后续必须配置签名机密、增加工作流强制校验，并使用新递增版本重新发布。
