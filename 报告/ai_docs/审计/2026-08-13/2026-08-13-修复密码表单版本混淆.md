# 修复密码表单版本混淆

- 任务名称：修复密码表单版本混淆
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`apps/desktop/src/App.tsx`、`apps/desktop/dist/`、`target/release/SSH-Forward-GUI-v0.1.0-dpapi.exe`

## 变更审查

- 本次仅增加 GUI 可见版本标识，并创建独立命名的 Release 可执行文件。
- 认证方式和密码输入逻辑未被移除；构建后的 JavaScript 资源经内容检查，确认包含“认证方式”“密码（加密保存到配置）”和版本标识。
- 未改动用户配置、密码、凭据或 SSH 连接设置。

## 交付风险

- 同一目录中仍保留旧 `target/debug/ssh-forward-desktop.exe`，它是开发构建且界面不包含本次功能；不得作为独立运行文件使用。
- 今后应使用版本化 Release 文件，避免操作系统快捷方式、历史命令或旧窗口启动旧 Debug 程序。
