# 修复桌面启动资源

- 任务名称：修复桌面启动资源
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`apps/desktop/src-tauri/tauri.conf.json`、`README.md`、`target/release/ssh-forward-desktop.exe`

## 变更范围审查

- 代码配置未修改；本次使用已有 Tauri 配置生成正确的 Release 构建产物，并仅更新 README 和归档文档。
- 生成的 Release 可执行文件及前端构建资源均位于项目目录的 `target/` 与 `apps/desktop/dist/`。
- 未触及 SSH Host、Tunnel 配置内容、密钥、凭据或外部主机。

## 原因与修复审查

- `target/debug/ssh-forward-desktop.exe` 属于 Tauri 开发构建，会读取 `devUrl` 的 `http://localhost:1420`；未启动 Vite 时出现 `ERR_CONNECTION_REFUSED` 是预期的开发模式行为。
- `tauri.cmd build --no-bundle` 生成 Release 模式的 `target/release/ssh-forward-desktop.exe`，其前端资源来自 `frontendDist`，可独立运行。
- README 已明确区分热更新开发、独立运行和安装包构建，避免后续误用 Cargo Debug 桌面端产物。

## 遗留风险

- 未进行完整的人工界面截图验证；已通过 Release 进程启动且没有本地 1420 端口监听验证其不依赖 Vite 服务。
- 安装包尚未生成，因为本次使用 `--no-bundle` 聚焦于修复可直接运行的应用程序。
