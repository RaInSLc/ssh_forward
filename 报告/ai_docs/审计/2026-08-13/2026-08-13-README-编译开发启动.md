# 补全 README 编译开发启动说明

- 任务名称：补全 README 编译开发启动说明
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`README.md`、`apps/desktop/package.json`、`apps/desktop/package-lock.json`

## 变更范围审查

- 修改范围仅为 README、桌面端开发依赖声明及本次归档文档。
- 新增 `@tauri-apps/cli` 作为 `apps/desktop` 的开发依赖，版本锁定在项目内 `package-lock.json`；没有使用或要求全局 Tauri CLI。
- README 的 CLI、GUI、检查和打包命令均与当前 Cargo workspace、Vite scripts 和 Tauri 配置一致。

## 风险与合规检查

- Windows PowerShell 环境中的 npm 包装对通过 npm script 转发 Tauri 子命令存在解析冲突；README 改用项目内 `node_modules/.bin/tauri.cmd`，该入口已验证可用。
- 文档明确 Tauri 开发和打包所需的 WebView2 Runtime 与 MSVC 工具链，避免把环境缺失表现为项目功能错误。
- 未新增敏感信息、外部主机、私钥内容或其他运行权限。

## 遗留事项

- 未实际执行 `tauri.cmd dev`，因为该命令会启动持续运行的开发服务器和桌面窗口；仅执行帮助命令验证入口与参数。
- 未实际执行 `tauri.cmd build`，因为该命令生成发布安装包；仅执行帮助命令验证入口与可用选项。
