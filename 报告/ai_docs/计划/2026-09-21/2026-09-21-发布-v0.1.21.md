# 发布 v0.1.21 计划

- 任务名称：发布 v0.1.21 并监控打包
- 日期：2026-09-21
- 状态：已完成（在线更新存在外部配置阻塞）
- 负责人：AI
- 关联文件：`Cargo.toml`、`apps/desktop/package.json`、`apps/desktop/src-tauri/tauri.conf.json`、`CHANGELOG.md`、`.github/workflows/ci.yml`、`.github/workflows/release.yml`

## 目标

将当前工作区已准备的 0.1.21 版本提交至 `main`，创建并推送 `v0.1.21` 标签，触发桌面端发布流水线，并确认最新版发布资产完整生成。

## 范围与前提

- 当前分支为 `main`，与 `origin/main` 同步；工作区有待发布的功能、测试、配置和版本变更。
- 三处产品版本号均为 `0.1.21`，`CHANGELOG.md` 已含对应条目。
- 远程最新标签为 `v0.1.18`，因此 `v0.1.21` 符合递增规则且不存在同名远程标签。
- GitHub CLI 已认证，可执行推送、查询工作流与查询 Release。

## 风险

- 本地检查可能暴露编译、测试、格式或 IPC 契约问题，发布将暂停并据实处理。
- 标签推送会触发不可逆的远程发布流程；仅在全部规定检查成功且标签不存在后执行。
- 云端 macOS/Windows 打包依赖 GitHub Actions 运行环境和仓库密钥，需在工作流结束后以实际状态为准。

## 实施与验证计划

1. 记录工作区状态、版本号、远程标签和发布工作流配置。
2. 执行版本与 IPC 契约检查，以及 Rust 格式、Clippy、工作区测试、前端类型检查、测试和构建。
3. 检查变更后创建发布提交，推送 `main`；确认远程不存在同名标签后创建并推送 `v0.1.21`。
4. 使用 GitHub CLI 监控标签对应的 Release 工作流，确认成功、Release 已发布及各平台资产存在。
