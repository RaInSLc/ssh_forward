# 补全 README 编译开发启动说明

- 任务名称：补全 README 编译开发启动说明
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`README.md`、`apps/desktop/package.json`、`apps/desktop/src-tauri/tauri.conf.json`

## 目标

补全 README 中 CLI 与桌面端的环境要求、依赖安装、开发启动、Debug/Release 编译、测试检查和 Windows 打包说明。

## 范围

- 修改 `README.md`，使命令与当前 Cargo workspace、npm scripts、Tauri 配置相符。
- 将 Tauri CLI 作为 `apps/desktop` 的开发依赖并提供本地 npm 脚本，使热更新开发命令无需全局安装。
- 不改变 SSH 转发功能、配置模型或 GUI 行为。

## 风险与验证

- Tauri 的开发和打包命令会依赖 Windows WebView2 与 MSVC 工具链；README 将明确此要求。
- `tauri build` 可能耗时较长且生成安装包，本次仅验证 CLI 可用与项目编译路径，不生成发布包。
- 验证 npm 脚本、前端检查、Cargo 构建及中文文档编码。
