# 修复密码表单版本混淆

- 任务名称：修复密码表单版本混淆
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`apps/desktop/src/App.tsx`、`target/release/`

## 目标

解决用户打开旧 Debug 桌面端后仍看不到认证方式和密码输入框的问题，并提供带可见版本标识的独立 Release 程序。

## 已知事实

- 截图中的表单只有私钥路径输入，与旧 GUI 一致。
- 当前源码包含认证方式、密码认证选项和密码输入字段。
- 旧 Debug 程序的构建时间早于当前 Release 程序，不应作为独立 GUI 交付物使用。

## 验证计划

显示 GUI 版本标识，构建独立命名的 Release 文件，并验证产物包含认证字段文本且可独立启动。
