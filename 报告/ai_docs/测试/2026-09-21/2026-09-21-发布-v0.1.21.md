# 发布 v0.1.21 测试记录

- 任务名称：发布 v0.1.21 并监控打包
- 日期：2026-09-21
- 状态：部分通过
- 负责人：AI
- 关联文件：`.github/workflows/release.yml`、GitHub Actions 运行 `35553661375`、GitHub Release `v0.1.21`

## 本地验证

| 命令 | 结果 |
| --- | --- |
| `node scripts/check-version.mjs` | 通过，三处版本定义和变更日志一致，版本为 0.1.21。 |
| `node scripts/check-ipc-contract.mjs` | 通过，15 个 Rust 命令与前端引用一致；2 个不可达命令已登记。 |
| `cargo fmt --all -- --check` | 通过。 |
| `cargo clippy --workspace -- -D warnings` | 通过。 |
| `cargo test --workspace` | 通过，67 项 Rust 测试通过。运行期间有 Windows 增量缓存 `os error 5` 警告，命令成功退出。 |
| `npm --prefix apps\\desktop run check` | 通过。 |
| `npm --prefix apps\\desktop test` | 通过，5 个测试文件、28 项测试通过。 |
| `npm --prefix apps\\desktop run build` | 通过，Vite 生产构建成功。 |

## 云端验证

- GitHub Actions `Release desktop app` 运行 `35553661375` 成功。
- Windows x64 构建成功，包含格式、Rust 测试、Clippy、前端验证、桌面打包和资产上传。
- macOS x64 与 macOS aarch64 构建成功，包含同等验证、桌面打包和资产上传。
- Release 汇总作业成功生成并上传发布资产。
- Release `v0.1.21` 已存在，安装资产包括 Windows MSI、NSIS、便携包和两种 macOS 的 DMG、便携包。

## 未通过项

- 下载并解析 `latest.json` 后发现 `platforms` 为 `{}`，未覆盖任何 Tauri Updater 平台。
- Release 无 `.sig` 签名资产，故不能验证在线更新下载与签名校验。原因和处理路径见关联问题记录。
