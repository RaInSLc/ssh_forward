# 补全 README 编译开发启动说明

- 任务名称：补全 README 编译开发启动说明
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`README.md`、`apps/desktop/package.json`、`apps/desktop/package-lock.json`

## 已执行验证

| 验证 | 命令或检查 | 结果 |
| --- | --- | --- |
| 项目内 Tauri CLI | `node_modules/.bin/tauri.cmd dev --help` | 通过 |
| 项目内 Tauri 打包命令 | `node_modules/.bin/tauri.cmd build --help` | 通过 |
| TypeScript 检查 | `npm run check` | 通过 |
| 前端生产构建 | `npm run build` | 通过 |
| CLI Debug 编译 | `cargo build -p ssh-forward` | 通过 |
| 桌面端 Rust 检查 | `cargo check -p ssh-forward-desktop` | 通过 |
| CLI 帮助 | `cargo run -p ssh-forward -- --help` | 通过 |
| README 内容 | 检查编译、开发、启动、打包和测试命令均存在 | 通过 |
| 中文编码 | README 与本次归档文档的 UTF-8 无 BOM、LF、替换字符检查 | 通过 |

## 未执行项

- 未实际运行 Tauri 开发窗口和安装包构建，原因是二者会启动持续进程或产生发布产物；本次目标是补全说明并验证命令入口。
- 未重复执行完整 Rust workspace 测试与 Clippy，因为本次未修改 Rust 源码；此前 GUI 任务已通过对应验证。
