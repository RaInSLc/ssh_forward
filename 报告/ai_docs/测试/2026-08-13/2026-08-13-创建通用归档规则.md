# 任务测试

- 任务名称：创建通用 AI Skill 与 Agent 归档规则
- 日期：2026-08-13
- 状态：已通过
- 负责人：AI
- 关联文件：`AGENTS.md`、`agent.md`、`.opencode/skills/ai-report-archive/SKILL.md`

## 验证计划

- 读取所有新增和更新的规则文件，确认内容和目标路径一致。
- 检查 Markdown 文件为 UTF-8、无 BOM，并只使用 LF 换行。
- 确认 Skill 文件包含有效的名称与描述前置元数据。

## 结果

已执行编码与结构检查，结果通过。

- 检查对象：根规则、Agent 入口、Skill 和本任务五类流程文档，共 8 个 Markdown 文件。
- 检查结果：全部可按 UTF-8 解码，无 UTF-8 BOM，仅使用 LF 换行，且不含替换字符。
- 内容检查：`AGENTS.md` 与 Skill 均包含 `报告/ai_docs/<流程类别>/YYYY-MM-DD/` 归档规则及 `报告/ai_codes/YYYY-MM-DD/` 辅助代码规则。
