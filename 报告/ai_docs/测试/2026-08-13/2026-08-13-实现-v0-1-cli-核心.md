# 实现 V0.1 CLI 核心

- 任务名称：实现 V0.1 CLI 核心
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`Cargo.toml`、`crates/`、`apps/cli/`、`examples/config.json`

## 已执行验证

| 验证 | 命令或检查 | 结果 |
| --- | --- | --- |
| Rust 格式 | `cargo fmt --all -- --check` | 通过 |
| 单元测试 | `cargo test --workspace` | 通过，3 项测试成功 |
| 静态检查 | `cargo clippy --workspace -- -D warnings` | 通过，无警告 |
| 示例配置 | `cargo run -p ssh-forward -- --config examples/config.json config validate` | 通过 |
| CLI 列表 | 使用示例配置执行 `host list`、`tunnel list` | 通过，输出与示例一致 |
| CRUD 冒烟 | 在 `target/verification-config.json` 执行 Host/Tunnel 新增、校验、删除 | 通过，临时配置已删除 |
| 安全限制 | 尝试新增 `0.0.0.0:18080` 本地绑定 | 按预期失败，提示 V0.1 仅允许回环地址 |
| 文档编码 | 解码、BOM、LF、替换字符检查 | 通过 |

## 未执行验证

- 未执行真实 `ssh-forward start <tunnel>` 端到端连接测试。原因是项目中没有被授权使用的 SSH 主机、账号、密钥或 Agent 身份；为避免外部操作和敏感信息处理，本次未尝试真实连接。
- 未验证 GUI、托盘、自动重连、远程转发和动态转发，因为它们不在本次 V0.1 CLI 核心实现范围内。
