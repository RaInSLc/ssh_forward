# 密码改为 DPAPI 配置存储

- 任务名称：密码改为 DPAPI 配置存储
- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`crates/ssh-forward-config/`、`apps/desktop/src-tauri/`、`target/release/ssh-forward-desktop.exe`

## 已执行验证

| 验证 | 命令或检查 | 结果 |
| --- | --- | --- |
| DPAPI 加解密 | 桌面端 `dpapi_round_trip_does_not_keep_plaintext_in_ciphertext` 单元测试 | 通过，密文不等于明文且可由当前用户解密 |
| 密文配置结构 | 配置 crate 密码认证测试 | 通过，密码模式要求 `encrypted_password` |
| Rust 单元测试 | `cargo test --workspace` | 通过，5 项测试成功 |
| Rust 格式与静态检查 | `cargo fmt --all`、`cargo clippy --workspace -- -D warnings` | 通过，无警告 |
| TypeScript 与前端构建 | `npm run check`、`npm run build` | 通过 |
| Tauri Release Build | `tauri.cmd build --no-bundle` | 通过，生成新版 Release GUI |
| 独立启动 | 启动 Release 程序、等待 3 秒并检查进程 | 通过，进程保持运行 |

## 未执行项

- 未创建或修改用户实际密码配置，未读取真实密码，未进行真实 SSH 认证与转发连接测试。
- 未进行人工 GUI 截图检查；独立启动测试完成后已关闭测试窗口。
