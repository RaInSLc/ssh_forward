# 初始化通用 AI 工作流规则

- 日期：2026-08-13
- 状态：静态检查通过
- 负责人：AI
- 关联文件：`AGENTS.md`、`agent.md`、`.opencode/skills/dated-reporting/SKILL.md`、`.opencode/agent/reporting-workflow.md`

## 验证项

| 检查 | 预期结果 | 状态 |
| --- | --- | --- |
| 必需文件存在 | 项目规则、skill、agent 和五份阶段报告均存在 | 通过：共检查 9 个文件，无缺失 |
| 编码 | 所有新增 Markdown 为 UTF-8 无 BOM | 通过：无 BOM、无替换字符 |
| 换行 | 所有新增 Markdown 仅使用 LF | 通过：未发现 CR 或 CRLF |
| OpenCode 加载 | 重启后的 OpenCode 发现 skill 和 agent | 未执行 |

## 未执行项

OpenCode 的运行时加载验证必须在重启后的会话中进行；本次已完成静态文件、编码和换行检查。

## 执行命令

使用 Python 对 9 个新增 Markdown 文件检查存在性、UTF-8 解码、BOM、回车字符和替换字符。首次命令因 PowerShell 解析内联换行失败；改用单行和 Unicode 路径转义后成功执行。
