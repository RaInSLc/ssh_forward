# 初始化通用 AI 工作流规则

- 日期：2026-08-13
- 状态：已完成
- 负责人：AI
- 关联文件：`AGENTS.md`、`agent.md`、`.opencode/skills/dated-reporting/SKILL.md`、`.opencode/agent/reporting-workflow.md`

## 执行记录

1. 读取 OpenCode 自定义配置规范，确认项目 skill 位于 `.opencode/skills/<名称>/SKILL.md`，agent 位于 `.opencode/agent/<名称>.md`。
2. 读取中文文件编码约束，按 UTF-8 无 BOM 和 LF 换行创建中文 Markdown 文档。
3. 新增项目级 `AGENTS.md`，定义日期归档、辅助代码隔离、阶段文档和完成门禁。
4. 新增 `agent.md` 便捷入口、`dated-reporting` skill 及 `reporting-workflow` 子 agent。
5. 创建本次任务的规划、操作、审计、测试和最终报告文档。
6. 首次静态校验命令受 PowerShell 内联换行与中文路径转义影响而解析失败；未修改任何文件。改用 Unicode 转义的单行 Python 校验命令后成功完成检查。
