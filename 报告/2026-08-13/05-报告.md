# 初始化通用 AI 工作流规则

- 日期：2026-08-13
- 状态：已完成，静态检查通过，待重启后运行时确认
- 负责人：AI
- 关联文件：`AGENTS.md`、`agent.md`、`.opencode/skills/dated-reporting/SKILL.md`、`.opencode/agent/reporting-workflow.md`

## 结果

已建立一套项目级通用流程：所有实质性 AI 任务均需按日期写入规划、操作、审计、测试和最终报告；AI 的非项目辅助代码被限定到 `报告/ai_codes/` 的日期化任务目录。

静态检查已确认 9 个新增 Markdown 文件存在、可按 UTF-8 解码、无 BOM、无替换字符且只使用 LF 换行。

## 交付物

- 项目级规则：`AGENTS.md` 和 `agent.md`。
- 可触发 skill：`.opencode/skills/dated-reporting/SKILL.md`。
- 可委派子 agent：`.opencode/agent/reporting-workflow.md`。
- 本次初始化的完整阶段记录：`报告/2026-08-13/`。

## 限制

已启动的 OpenCode 会话不会热加载这些配置类文件。重启后应确认 skill 和 agent 可被发现。
